🔑 Роли пользователей
Admin:

Управляет пользователями (блокировка, удаление, смена ролей).

Может управлять всеми товарами.

Seller:

Создаёт, редактирует и удаляет свои товары.

Просматривает список своих товаров.

User:

Регистрируется, логинится.

Просматривает каталог товаров.

Добавляет товар в корзину.

Удаляет товар из корзины.

Оформляет заказ.

Просматривает историю заказов.

Выходит из аккаунта.

🖼 UI (фронт)
/login — логин по email и паролю.

/register — регистрация.

/catalog — каталог товаров (поиск, фильтрация, пагинация).

/cart — корзина пользователя.

/profile — профиль с кнопкой «выйти», историей заказов.

/seller/products — товары селлера.

/admin/users — список пользователей, кнопки «удалить», «заблокировать», «изменить роль».

/product/:id/edit — редактирование товара (доступно только владельцу/админу).

/checkout — оформление заказа.

🔌 API (бек, примеры эндпоинтов)
Аутентификация:
POST /auth/register — регистрация.

POST /auth/login — логин, возвращает токен или устанавливает cookie.

POST /auth/logout — завершение сессии.

GET /auth/profile — данные текущего пользователя.

Пользователи:
GET /admin/users — список пользователей.

PATCH /admin/users/{id} — обновление ролей или блокировка.

DELETE /admin/users/{id} — удаление пользователя.

Товары:
GET /products — список товаров с фильтрами.

POST /products — создание товара (только seller или admin).

GET /products/{id} — детальный просмотр товара.

PATCH /products/{id} — редактирование (только владелец или admin).

DELETE /products/{id} — удаление.

Корзина:
GET /cart — текущая корзина.

POST /cart/add — добавить товар в корзину.

POST /cart/remove — удалить товар из корзины.

Заказы:
POST /checkout — оформить заказ.

GET /orders — история заказов.

Отзывы:
POST /products/{product_id}/reviews - Добавить отзыв (только юзер)
GET /products/{product_id}/reviews - Получить список отзывов по товару - Все
PATCH /reviews/{review_id} - Обновить свой отзыв - Только автор
DELETE /reviews/{review_id} - Удалить свой отзыв - Только автор
DELETE 	/admin/reviews/{review_id} - Удалить любой отзыв - Только админ


💡 Особенности
Аутентификация через JWT или cookie-сессии.

Проверка ролей на бэке.

Простейшая SQLite или PostgreSQL БД (с таблицами users, products, orders, cart).

Минималистичный UI на React/Vue или даже просто HTML-формы — важнее API.

🧪 Что можно потренировать на автотестах
✅ API-тестирование: запросы на регистрацию, логин, CRUD по товарам, проверка прав доступа.

✅ UI автотесты (например, Playwright):

Проверка логина, навигации, наличия товаров.

Проверка, что юзер не может редактировать чужие товары.

✅ Mock API: протестировать UI без реального бэка.

✅ Ролевая модель: запуск тестов от разных пользователей.

✅ Параметризация, фикстуры, динамика.

🖼 UI страницы для отзывов
Внутри /product/:id
Блок «Отзывы»

Отображение текста отзыва, пользователя, рейтинга, даты.

Кнопка «Удалить» / «Редактировать» — только если автор (или админ).

Форма «Оставить отзыв» — если пользователь залогинен и не оставлял отзыв ранее.